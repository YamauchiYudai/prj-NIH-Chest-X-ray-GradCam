name: CI/CD Pipeline

on:
  push:
    branches: [ "develop" ]
    tags: [ "v*.*.*" ]
  pull_request:
    branches: [ "develop" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ------------------------------------------------------------------
  # JOB 1: Test (CI)
  # ビルドが可能か、スクリプトがエラーなく動作するかを確認します
  # ------------------------------------------------------------------
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Dockerのセットアップ
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # データセットがないCI環境用にダミーのリストファイルを作成
      # (dataset.pyは画像がない場合自動でダミー画像を生成するため、リストさえあれば動きます)
      - name: Create dummy data lists for CI
        run: |
          mkdir -p data
          echo "00000001_000.png" > data/train_val_list.txt
          echo "00000001_000.png" > data/test_list.txt

      # Docker Composeを使ってビルド＆検証スクリプト実行
      - name: Build and Run Smoke Test
        run: |
          docker compose build
          docker compose run --rm app python verify_pipeline.py

  # ------------------------------------------------------------------
  # JOB 2: Build & Push (CD)
  # テストがパスし、かつPRでない場合にDockerイメージをレジストリにプッシュします
  # ------------------------------------------------------------------
  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Dockerメタデータ（タグ名など）の抽出
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,format=long

      # イメージのビルドとプッシュ
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
